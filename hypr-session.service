# ~/.config/systemd/user/hypr-session.service
[Unit]
Description=Hyprland window manager session
PartOf=hypr-session.target
Wants=hypr-session.target

# this service is started by hyprland in its config file
# it's done like this so sway and its launched child processes
# are not a service itself, but in the regular session scope.
[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/true
Restart=on-failure
